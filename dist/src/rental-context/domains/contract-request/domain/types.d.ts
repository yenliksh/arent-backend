export declare enum ContractRequestStatus {
    REJECTED = "REJECTED",
    CREATED = "CREATED",
    ACCEPTED = "ACCEPTED"
}
export declare type ContractRequestCursorType = {
    id: string;
    createdAt: Date;
};
